import { RemovePartnerResult } from './remove-partner-result.dto';

describe('RemovePartnerResult', () => {
  it('should be defined', () => {
    expect(new RemovePartnerResult()).toBeDefined();
  });
});
